{
  "enabled": true,
  "name": "Compliance Review Trigger",
  "description": "Trigger compliance review when source files are saved. Reviews against MAS TRM, PCI-DSS, and PDPA requirements.",
  "version": "1",
  "when": {
    "type": "fileSaved",
    "patterns": ["src/**/*.java", "src/**/*.ts", "src/**/*.tsx"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Review the saved file against the active compliance steering (MAS TRM, PCI-DSS, PDPA). Flag any violations with severity level and the specific regulation reference."
  }
}
