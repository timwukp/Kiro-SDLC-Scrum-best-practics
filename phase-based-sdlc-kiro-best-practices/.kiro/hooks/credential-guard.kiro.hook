{
  "enabled": true,
  "name": "Credential Guard",
  "description": "Block writes containing hardcoded credentials, API keys, or private keys. Enforces AWS Secrets Manager usage.",
  "version": "1",
  "when": {
    "type": "preToolUse",
    "toolTypes": ["write"]
  },
  "then": {
    "type": "runCommand",
    "command": ".kiro/hooks/scripts/scan-credentials.sh"
  }
}
